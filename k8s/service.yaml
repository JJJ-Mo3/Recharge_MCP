apiVersion: v1
kind: Service
metadata:
  name: recharge-mcp-service
  namespace: recharge-mcp
  labels:
    app: recharge-mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: recharge-mcp-server